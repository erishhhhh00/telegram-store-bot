{
  "name": "telegram-store-bot",
  "version": "1.0.0",
  "description": "Pro-level Telegram Store Bot with QR Payments and Vercel Serverless.",
  "main": "api/webhook.js",
  "scripts": {
    "start": "node src/dev.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "mongoose": "^8.3.0",
    "telegraf": "^4.16.3"
  },
  "engines": {
    "node": ">=18.x"
  }
}
